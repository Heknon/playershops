package me.oriharel.playershops.shops.shop

enum class ShopSetting {
    USE_MOB_COINS,
    USE_INTERNAL_BANK // this setting implies that a shop should store money inside it self instead of directly depositing to owner
}